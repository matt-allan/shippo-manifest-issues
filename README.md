# Setup

1. Run `npm install` to install dependencies
2. Copy the example `.env` file: `cp .env.example .env`
3. Fill out the missing environment variables in `.env`
4. Run the script: `node ./index.js`